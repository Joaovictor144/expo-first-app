import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
      texto: {
        fontSize:20,
        fontWeight: 'bold',
      },
      input:{
        borderColor: '#00FFC9',
        borderWidth: 1,
        padding: 1,
        
      },

});